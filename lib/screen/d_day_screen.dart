import 'package:flutter/material.dart';

class DDayScreen extends StatelessWidget {
  const DDayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('D-Day Screen'),
      ),
    );
  }
}
